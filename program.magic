# Implementierung RSA-Algorithmus

# 1. Primzahlen bestimmen (eig. moeglichst gross)
p = 17;
q = 11;

# 2. n berechnen
n = p * q;

# 3. phi(n) berechnen
phi = (p-1) * (q-1);

# 4. e festlegen mit der Annahme e ist teilerfremd zu phi(n) ausgedrueckt: gcd(e, phi(n))=1
e = 7; 
 
magic egcd(a, b){
  if( b == 0){
    return 1;
  }else{
    return 0;
  }
}






